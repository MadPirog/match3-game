// Placeholder for scene management (if we later adopt a scene-based architecture)
export class BootScene {
  constructor(game) {
    this.game = game;
  }
  init() {
    // Load assets, then proceed to menu
    console.log('BootScene: initializing');
  }
}